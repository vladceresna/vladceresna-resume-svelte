import { describe, it, expect } from 'vitest';
// This is a simple test to demonstrate the setup
// TODO: Add more complex tests as needed
describe('sum test', () => {
	it('adds 1 + 2 to equal 3', () => {
		expect(1 + 2).toBe(3);
	});
});
